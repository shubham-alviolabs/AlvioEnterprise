export interface NavItem {
  label: string;
  href: string;
}

export interface FeaturePoint {
  title: string;
  description: string;
}

export enum SectionTheme {
  Dark = 'dark',
  Glass = 'glass',
}